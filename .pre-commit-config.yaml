# Run `pre-commit install` once after cloning. Hooks auto-run on `git commit`.
# Docs: https://pre-commit.com
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.2
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format